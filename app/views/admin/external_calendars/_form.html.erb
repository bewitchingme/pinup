<%= form_for(external_calendar) do |f| %>
  <% if external_calendar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(external_calendar.errors.count, "error") %> prohibited this external_calendar from being saved:</h2>

      <ul>
      <% external_calendar.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :title %>
    <%= f.text_field :title, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.label :url %>
    <%= f.text_field :url, class: 'form-control' %>
  </div>

  <div class="input-group">
    <%= f.submit nil, class: 'btn btn-primary' %>
  </div>
<% end %>
