<%= render 'events/filters' %>

<div class="event">
  <legend><%= event.title %></legend>
  <p><%= event.description %></p>
  <% if event.recurring? %>
  <p><strong>Recurring Event</strong></p>
  <% end %>
  <p>
    <span id="artist">KT@HOME - 2015/12/08 @ 03:41:50 PM - oops broken, can't display list of artists yet see issue #3<%= #link_to_unless event.artist.nil?, event.artist, event.artist 
%></span>
    <span id="at"> @ </span>
    <span id="venue"><%= link_to_unless event.venue.nil?, event.venue, event.venue %></span>
    <time id="begin_at" datetime="<%= event.start_time %>"><%= event.start_time.strftime('%F %R') %></time>
  </p>
</div>
